# 招聘数据分析项目（拉勾网 · 数据分析岗）

> 一句话：分析拉勾网 543 条数据分析岗位的公开招聘数据，回答"数据岗在城市、学历、经验、
> 技能上到底有什么要求"。这个项目就是简历上"互联网岗位招聘数据分析（个人项目）"的实体。

## 目录结构

```
job_analysis/
├── 01_采集练习_豆瓣.py      # 爬虫练习（已验证可跑，产出不进入主线分析）
├── 02_清洗数据.py           # 原始 xlsx → 干净 CSV
├── 03_导入MySQL.py          # CSV → MySQL（运行前先填你的 root 密码）
├── 04_分析.sql              # 7 段 SQL，在 MySQL Workbench 里逐段运行
├── 05_技能词频分析.py       # JD 技能提及率分析（已修复词边界 bug）
├── 诊断_两种口径对比.py      # 验证脚本：以 CSV 为事实来源核对各查询真实结果
├── requirements.txt         # 依赖清单（已装好）
└── data/
    ├── raw/
    │   ├── Data_Lagou.xlsx              # 主数据集（GitHub 公开数据集，勿删）
    │   └── douban_top250_练习.csv       # 01 脚本的练习产出
    └── clean/
        ├── jobs_clean.csv               # 清洗后 543 条（Power BI 用这个）
        └── skill_rates.csv              # 技能出现率表（核心图用这个）
```

## 环境状态（2026-09-05 已配好）

| 组件 | 状态 |
|---|---|
| Python 3.12.10 | 已安装，pandas / requests / pymysql / sqlalchemy / bs4 / openpyxl 全部就绪 |
| MySQL Server 8.0 | 已安装且服务运行中（服务名 MySQL80），Workbench 图形界面也有 |
| Power BI Desktop | **未安装，需要你手动装**（见下） |

**运行方式统一用 `py` 命令**（例如 `py 02_清洗数据.py`）。
不要用 `python` 命令——你电脑里还残留 Python 3.5/3.7 和商店占位符，会弹应用商店。

## 运行顺序

```
cd Desktop\job_analysis

py 01_采集练习_豆瓣.py     # 可选练习，已验证：能采到 50 条
py 02_清洗数据.py          # 已验证：589 条 → 543 条
py 05_技能词频分析.py      # 已验证：技能出现率表 + 经验交叉分析
py 03_导入MySQL.py         # ← 先用编辑器打开它，把 MYSQL_PASSWORD 改成你的 root 密码
                            #   运行成功后，打开 MySQL Workbench 跑 04_分析.sql
```

## 你需要手动做的两件事

**1. 把 MySQL root 密码填进 03_导入MySQL.py**
- 就是当初安装 MySQL 时自己设置的那个密码
- 忘了的话告诉我，我带你走官方重置流程

**2. 安装 Power BI Desktop**（自动安装失败，因为微软商店源在你的网络环境被限制）
- 方式一：打开 Microsoft Store 应用，搜 "Power BI Desktop"，点安装
- 方式二（商店不可用时）：微软官网下载页 `microsoft.com/zh-cn/download/details.aspx?id=58494`，下 PBIDesktopSetup.exe 安装
- 进阶：想让 Power BI 直接连 MySQL，需另装 MySQL Connector/NET（mysql.com 有下载）；
  嫌麻烦就直接连 CSV——数据完全一样，只是简历上写"Power BI 连接数据源"不特指 MySQL

## Power BI 看板（半天搞定，建议 4 张图 + 联动）

1. **条形图**：城市 × 岗位数（数据源 jobs_clean.csv）——体现需求集中在哪
2. **条形图**：技能 × JD出现率%（数据源 skill_rates.csv）——**这是核心图**
3. **卡片图**：总岗位数 543、平均薪资 13.1k
4. **切片器**：城市、经验要求——点了切片器，所有图自动只算筛选后的数据
   （这就是"筛选上下文"，Power BI 面试必问概念）

## 数据来源与局限（面试时要主动说，加分项）

- **来源**：GitHub 公开数据集 `BigCarrey/Data_analysis_in_lagou`（Data_Lagou.xlsx），
  拉勾网"数据分析"类岗位，2016-09 采集，589 条原始数据
- **局限一**：数据是 2016 年的，薪资绝对值只代表当时水平；结论应侧重**结构和相对关系**
  （技能要求排序、城市分布、经验与薪资的关系），这个意识本身就是面试加分项
- **局限二**：技能词频是关键词匹配，"熟悉 SQL"和"精通 SQL"未区分
- **局限三**：交叉分析里"经验 10 年以上 / 1 年以下"分组样本太小，0% 不可信——
  这正是 04_分析.sql 里 `HAVING COUNT(*) >= 10` 存在的意义
- **迭代方向**：用 01 的思路采集 2024+ 新数据，或找更新的公开数据集替换
  `data/raw/` 里的文件 + 改 02 的 `COLUMN_MAP`，主线代码不用动

## 踩坑记录（真实踩过，面试讲出来最加分）

1. **小样本陷阱（出现了三次）**：岗位类别里"其他/运营"仅 2 条却以 59.5k 的平均薪资居首；
   查询 6 里宁波 2 条以 50k 排第一；经验"10 年以上"仅 1 条 45k。解法都是 `HAVING COUNT(*) >= 10`
   过滤小样本组，或在报告里明确标注样本量
2. **正则词边界在中文语境失效**：`\bExcel\b` 匹配不到"熟练Excel"——Python 把汉字也算
   单词字符，"练"和"E"之间不存在词边界，导致 Excel 提及率被低估 5 倍（7% vs 38%）。
   解法：`(?<![A-Za-z])技能(?![A-Za-z])` 只按英文字母判边界
3. **口径必须声明**：SQL 的 `LIKE '%SQL%'` 会把 "MySQL" 也计入（44%），独立提及口径是 40%。
   两个数都对、口径不同；任何对外引用的数字都要能说清"怎么算的"
4. **读数要读源头**：项目过程中曾因看截图读错数字得出错误结论（"数据工程是数据分析 2.2 倍"，
   实际 1.35 倍），最终以直接对数据重算为准——永远别相信二手转述，包括自己的眼睛

## 面试自测 5 问（跑完全流程后，用自己的话写下来）

1. **数据从哪来？多少条？什么时间？**
   → GitHub 公开数据集，拉勾网数据分析岗 589 条，2016-09 采集
2. **去重规则是什么？为什么？**
   → "职位名称+公司"组合相同视为重复抓取，删除 45 条；再剔除 1 条薪资无法解析的（面议），得 543 条
3. **"10k-20k"这种薪资你怎么处理的？**
   → 正则捕获组拆出上下限，取区间中值（15k）代表岗位水平；年终奖系数（·13薪）未计入；
   无法解析的格式（面议/10k以上）剔除并记录数量
4. **得出了什么结论？有反直觉的吗？**
   → 城市侧：北上深集中 73% 岗位，但深圳平均薪资（16.0k）最高、超过岗位最多的北京（14.0k）；
   上海（12.9k）甚至低于全站平均（13.1k）——岗位多 ≠ 薪资高
   → 经验侧：薪资阶梯清晰，应届 5.8k → 1-3 年 10.8k → 3-5 年 17.2k → 5-10 年 27.1k
   → 技能侧：SQL（40%）、Excel（38%）、数据挖掘（34%）居前三，SAS/SPSS（27%）高于
   Python（20%）——2016 年国内数据岗偏统计分析工具，对比今天 Python/SQL 为主流，
   可见技能栈迭代速度（"对比今昔"是面试好素材）
5. **重做一遍你会改进什么？**
   → 更新数据到 2024+、区分技能熟练度层级、扩大岗位范围（加产品/运营岗做对比分析、
   支持投三个方向的叙事）

## 简历措辞（数字已替换为真实值，跑完 04 后补最后一处）

```
互联网「数据分析」岗位招聘数据分析（个人项目）                     2026.09 – 至今
- 基于拉勾网 589 条数据分析岗公开招聘数据（GitHub 公开数据集），使用 Pandas 完成
  去重（45 条）、缺失值处理与薪资区间解析（"10k-20k"→中值），产出 543 条标准化数据
- 数据导入 MySQL，编写 SQL 从城市、学历、经验、岗位类别 4 个维度输出分析结论：
  北上深集中 73% 岗位而深圳薪资（16.0k）反超岗位最多的北京（14.0k）；经验薪资阶梯清晰
  （应届 5.8k → 3-5 年 17.2k）；数据工程类（16.4k）较数据分析类（12.2k）高约 35%
- 技能词频分析（含口径治理）：定位并修复正则词边界在中文语境失效导致的严重低估
  （Excel 提及率 7%→38%），修复后与 SQL LIKE 口径交叉验证一致；SQL（40%）、Excel（38%）、
  数据挖掘（34%）为 JD 最高频要求，SAS/SPSS（27%）高于 Python（20%），据此形成
  2016→2026 技能栈变迁洞察
- 产出 Power BI 交互看板（城市/技能/薪资 3 类视图 + 切片器联动）
```

之后若采集了新数据，第一条改为"使用 Python 采集 XX 条……"，项目含金量再上一档。

## 下一步（按优先级）

1. 填密码 → `py 03_导入MySQL.py` → Workbench 跑 04（约 1 小时）
2. 装 Power BI → 连 CSV 出 4 张图（约半天）
3. 把自测 5 问的答案用自己的话写一遍——**这步最重要，面试全靠它**
4. 上传 GitHub：装 GitHub Desktop（图形界面最简单）或让助手装 git 命令行
5. 迭代数据源到 2024+（可选，做完简历第一条升级为"采集"）
